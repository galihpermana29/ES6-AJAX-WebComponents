//* setTimeout() digunakan ketika kita ingin menjalankan suatu kode setelah menunggu sejumlah waktu tertentu

setTimeout(() => {
    console.log('Setelah 3 detik')
}, 3000)

//* setInterval digunakan ketika kita ingin menjalankan kode secara berulang ulang, bisa terus menerus atau sampai waktu tertentu

/* const waktu = setInterval(() => {
    console.log('Setiap 3 detik sekali');
    clearInterval(waktu);
}, 3000); */

